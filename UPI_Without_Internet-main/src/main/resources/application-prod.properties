# Production profile — override via spring.profiles.active=prod
# Railway sets PORT automatically; CORS must be set via env var.

# H2 console disabled in production
spring.h2.console.enabled=false

# Disable dev SQL logging in production
spring.jpa.show-sql=false
logging.level.com.demo.upimesh=WARN
